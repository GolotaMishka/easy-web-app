import React, { ReactElement } from 'react';

import s from './styles.scss';

const Trajectory = (): ReactElement => {
  return <div className={s.page}>Trajectory</div>;
};
export default Trajectory;
